# Copy this file to .env.local for local development.
# In Vercel: Project Settings → Environment Variables.
# Anything starting with VITE_ is visible in the browser — never put secrets here.

# Temporary admin gate. Change this before sharing the live URL.
# This is a keep-out sign, not real security. Swap for Supabase auth before real client data.
VITE_ADMIN_PASSCODE=gracefield
